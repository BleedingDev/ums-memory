# Effect npm Version Availability

As of March 1, 2026, `npm view effect version` returns `3.19.19`.

`effect` v4 is not currently available on npm, so this baseline uses `effect@^3.19.19` while establishing typed services and composed layers that can be upgraded later.
